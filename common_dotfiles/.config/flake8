# vim: filetype=toml
[flake8]
extend-ignore = E203,E309,E402,E501,W291,W293,W391,W503
exclude =
    # No need to traverse our git directory
    .git,
    # There's no value in checking cache directories
    __pycache__,
    # The conf file is mostly autogenerated, ignore it
    docs/source/conf.py,
    # The old directory
    old,
    # The draft codes
    try, draft,
    # This contains our built documentation
    build,
    # This contains builds of flake8 that we don't want to check
    dist,
    # Testing
    .tox,
    # Python builds
    .eggs, *.egg
max-complexity = 18
max-line-length = 88
