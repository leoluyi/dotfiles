# vim: ft=sh:
# shellcheck shell=bash
# AI shell integrations via llm CLI (https://llm.datasette.io/)

command -v llm >/dev/null || return 0

LLM_MODEL="gpt-5.6-luna"
# Reasoning effort: none|minimal|low|medium|high|xhigh. Empty disables the flag.
# Only models registered with `reasoning: true` accept this option.
LLM_REASONING_EFFORT="xhigh"
_LLM_CMD_SYSTEM="Output only a single bash command with no explanation, no markdown, and no backticks. The command must run on $(uname -s)."

# Print model setup instructions when the configured model is unavailable.
_llm_model_hint() {
    cat >&2 <<EOF
Model '${LLM_MODEL}' not found. Check what is available:
  llm models                                            # ids llm knows about
  llm openai models                                     # ids the API serves

A model the API serves but llm does not list is newer than llm's bundled
registry; register it by hand in:
  $(dirname "$(llm keys path)")/extra-openai-models.yaml

Other providers need a plugin plus a key:
  llm install llm-anthropic && llm keys set anthropic   # Claude
  llm install llm-gemini    && llm keys set gemini      # Gemini
  https://github.com/simonw/llm
EOF
}

# Wrapper around llm that applies the configured model and reasoning effort,
# and prints setup instructions on 'Unknown model' errors. Callers must not pass
# -m themselves. Passes stdin and all args through; only stderr is intercepted.
_llm() {
    local tmp_err rc
    local -a opts=(-m "${LLM_MODEL}")
    [ -n "${LLM_REASONING_EFFORT}" ] && opts+=(-o reasoning_effort "${LLM_REASONING_EFFORT}")
    tmp_err=$(mktemp)
    llm "${opts[@]}" "$@" 2>"$tmp_err"
    rc=$?
    if [ $rc -ne 0 ] && grep -q "Unknown model" "$tmp_err" 2>/dev/null; then
        cat "$tmp_err" >&2
        _llm_model_hint
    else
        cat "$tmp_err" >&2
    fi
    rm -f "$tmp_err"
    return $rc
}

# Generate a shell command from a natural-language description.
# Usage: llm-cmd "list all files modified in the last 24 hours"
llm-cmd() {
    if [ -z "$1" ]; then
        echo "Usage: llm-cmd <natural language description>" >&2
        return 1
    fi
    _llm \
        --system "${_LLM_CMD_SYSTEM}" \
        "$*"
}

# Explain a command or piped content.
# Usage: llm-explain "find . -name '*.py' -exec grep foo {} +"
# Usage: some-command 2>&1 | llm-explain
llm-explain() {
    local input
    if [ -t 0 ]; then
        if [ -z "$1" ]; then
            echo "Usage: llm-explain <command>  OR  <cmd> | llm-explain" >&2
            return 1
        fi
        input="$*"
    else
        input="$(cat)"
        [ -n "$1" ] && input="Command: $* -- Output: $input"
    fi
    _llm \
        --system "You are a concise shell expert. Explain the following bash command or output clearly and briefly." \
        "$input"
}

# Pipe a failing command's output for a fix suggestion.
# Usage: some-command 2>&1 | llm-fix
llm-fix() {
    local input
    if [ -t 0 ]; then
        echo "Usage: <failing-command> 2>&1 | llm-fix" >&2
        return 1
    fi
    input="$(cat)"
    _llm \
        --system "You are a concise shell expert. Given this error output, provide the corrected bash command or a brief fix. Output only the fix, no extra explanation." \
        "$input"
}

# Generate a commit message from staged changes.
# Summarising a diff does not benefit from deep reasoning, so both git-* helpers
# pin effort to low regardless of LLM_REASONING_EFFORT.
# Usage: git-msg
git-msg() {
    local diff
    diff=$(git diff --cached)
    if [ -z "$diff" ]; then
        echo "No staged changes. Run \`git add\` first." >&2
        return 1
    fi
    echo "$diff" | LLM_REASONING_EFFORT=low _llm \
        --system "You are an expert at writing git commit messages. Output only a single conventional commit message (type: subject, optional body). No markdown, no backticks, no extra explanation." \
        "Write a commit message for this diff:"
}

# Generate a commit message and optionally commit.
# Usage: git-commit-ai
git-commit-ai() {
    local diff msg confirm
    diff=$(git diff --cached)
    if [ -z "$diff" ]; then
        echo "No staged changes. Run \`git add\` first." >&2
        return 1
    fi
    msg=$(echo "$diff" | LLM_REASONING_EFFORT=low _llm \
        --system "You are an expert at writing git commit messages. Output only a single conventional commit message (type: subject, optional body). No markdown, no backticks, no extra explanation." \
        "Write a commit message for this diff:")
    [ -z "$msg" ] && return 1
    echo "Proposed: $msg"
    read -r -p "Use this message? [y/N] " confirm
    [[ $confirm == [yY] ]] && git commit -m "$msg"
}

# Alt+a: replace current readline line (treated as natural language) with a
# generated bash command. Leaves the original text intact on failure and reports
# why -- a swallowed error here is indistinguishable from a dead keybinding.
_llm_inline_suggest() {
    [ -z "$READLINE_LINE" ] && return
    local suggestion
    suggestion=$(_llm --system "${_LLM_CMD_SYSTEM}" "$READLINE_LINE")
    if [ -n "$suggestion" ]; then
        READLINE_LINE="$suggestion"
        READLINE_POINT=${#READLINE_LINE}
    else
        echo "llm: no suggestion returned (model '${LLM_MODEL}')" >&2
    fi
}
# Only bind in interactive shells — readline is not available in non-interactive contexts.
[[ $- == *i* ]] && bind -x '"\ea": _llm_inline_suggest'

# Claude Code - project-free chat in a disposable temp directory.
claude-chat() {
  (cd "$(mktemp -d)" && claude "$@")
}
