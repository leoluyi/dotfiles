return {
  -- {
  --   -- easily jump to any location and enhanced f/t motions for Leap.
  --   -- < https://github.com/phaazon/hop.nvim >
  --   'phaazon/hop.nvim',
  --   branch = 'v2', -- optional but strongly recommended
  --   keys = function ()
  --     -- local hop = require('hop')
  --     -- local directions = require('hop.hint').HintDirection
  --
  --     return {
  --       { '<localleader><localleader>', '<cmd>HopWord<CR>', desc = "Hop Word" },
  --       -- { 'f', function() hop.hint_char1({ direction = directions.AFTER_CURSOR, current_line_only = true }) end, },
  --       -- { 'F', function() hop.hint_char1({ direction = directions.BEFORE_CURSOR, current_line_only = true }) end, },
  --       -- { 't', function() hop.hint_char1({ direction = directions.AFTER_CURSOR, current_line_only = true, hint_offset = -1 }) end, },
  --       -- { 'T', function() hop.hint_char1({ direction = directions.BEFORE_CURSOR, current_line_only = true, hint_offset = 1 }) end, },
  --     }
  --   end,
  --   opts = {
  --     keys = 'etovxqpdygfblzhckisuran',
  --   },
  --   config = function(_, opts)
  --     require'hop'.setup(opts)
  --   end
  -- },

}
