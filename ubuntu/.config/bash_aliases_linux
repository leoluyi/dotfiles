# ====== Linux only ======

# https://github.com/Mayccoll/Gogh
alias term-color='bash -c  "$(wget -qO- https://git.io/vQgMr)"'

[ -d "$HOME/workspace/repos" ] \
  && alias repo='cd "$HOME/workspace/repos"'

[ -d "$HOME/.dotfiles" ] \
  && alias dotfiles='cd "$HOME/.dotfiles"'
